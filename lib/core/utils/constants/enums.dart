/// Enum untuk status request
enum RequestStatus {
  initial,
  loading,
  success,
  error,
  empty,
}

/// Enum untuk status connection
enum ConnectionStatus {
  connected,
  disconnected,
  connecting,
}

/// Enum untuk theme mode
enum AppThemeMode {
  light,
  dark,
  system,
}

/// Enum untuk language
enum AppLanguage {
  id,
  en,
}

/// Enum untuk sort order
enum SortOrder {
  ascending,
  descending,
}

/// Enum untuk filter type
enum FilterType {
  all,
  active,
  inactive,
  archived,
}

